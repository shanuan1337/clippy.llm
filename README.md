# 📎 Клиппи — офисный помощник для Windows

Классический помощник из Microsoft Office 97, который следит за активным окном, распознаёт текст через Surya OCR и комментирует происходящее с помощью локальной LLM.

##  Возможности

-  Анимированная скрепка поверх всех окон (GIF)
-  Речевое облачко с хвостиком
-  Автоматический скриншот активного окна
-  Распознавание текста через Surya OCR (локальное развертывание с кастомным API)
-  Генерация шуток/комментариев через LLM
-  Перетаскивание окна мышью

##  Структура проекта

```text
C:\Programs\clippy.llm
├── clippy_overlay.py  # Главный скрипт
├── clippy.gif         # Анимация скрепки (GIF)
└── README.md          # Этот файл
```

> **Важно:** Пути в коде **абсолютные** (`C:\Programs\clippy.llm\`). Весь проект должен лежать именно в этой папке.

##  Требования

| Компонент | Версия / Описание |
| :--- | :--- |
| **Python** | 3.14 (работает на этой версии, но должна работать и на 3.11+) |
| **ОС** | Windows 10 / 11 |

## Установка зависимостей

Откройте командную строку **от имени администратора** и выполните:

```cmd
cd C:\Programs\clippy.llm
python -m pip install --upgrade pip
python -m pip install PyQt5 mss pywin32 requests
```

## Настройка параметров

Откройте `clippy_overlay.py` и найдите в начале файла секцию `НАСТРОЙКИ`:

```python
# ========== НАСТРОЙКИ ==========
API_URL = "OCR_API_URL"            
LLM_API_URL = "LLM_API_URL"
LLM_TOKEN = "LLM_TOKEN"
LLM_MODEL = "LLM_MODEL_NAME"
INTERVAL_SECONDS = 15                              # Частота проверки (сек)
SYSTEM_PROMPT = """..."""                          # Промпт для LLM

```

###  Регулировка частоты появления фраз

Измените `INTERVAL_SECONDS`:

```python
INTERVAL_SECONDS = 30   # Каждые 30 секунд (меньше — чаще, больше — реже)
```

###  Регулировка времени показа облачка

В функции `show_bubble` измените таймаут (в миллисекундах):

```python
QTimer.singleShot(15000, self.bubble.hide)   # 15000 = 15 секунд
```

*Примеры:*
*   `5000` = 5 секунд
*   `30000` = 30 секунд

##  Запуск

```cmd
cd C:\Programs\clippy.llm
python clippy_overlay.py
```

##  Лицензия

**MIT** — делайте что хотите, но скрепку не обижайте.